import React from "react";
import { LoadingContainer } from "./StyledCocktails";

const Loading = () => {
  return (
    <LoadingContainer>
      <h1>Loading...Loading...Loading...</h1>
    </LoadingContainer>
  );
};

export default Loading;
